# This component contains the protobuf message definitions.
# The actual protobuf source generation is handled at the project root level.
# This component primarily serves to group the .proto files.
idf_component_register(INCLUDE_DIRS ".")